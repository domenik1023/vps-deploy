# <NAME> - [local]. On the LAN: no SSH port move, no CrowdSec.
ansible_host: <ADDRESS>
ansible_port: 22

# Optional. Prometheus endpoints already served on this host.
# `name` is a bare Alloy identifier, `address` a bare host:port.
# alloy_extra_scrape_targets:
#   - { name: "example", address: "127.0.0.1:9100" }
